namespace EmuSync.Services.LudusaviImporter;

public enum Bit
{
    _32 = 32,
    _64 = 64
}

public enum Os
{
    dos,
    linux,
    mac,
    windows
}

public enum Store
{
    discord,
    epic,
    gog,
    microsoft,
    origin,
    steam,
    uplay
}

public enum Tag
{
    config,
    save
}